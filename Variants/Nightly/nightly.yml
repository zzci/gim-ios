settings:
  APP_GROUP_IDENTIFIER: group.im.g.message.nightly
  BASE_BUNDLE_IDENTIFIER: im.g.message.nightly

targets:
  ElementX:
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: NightlyAppIcon
    sources:
    - path: Resources